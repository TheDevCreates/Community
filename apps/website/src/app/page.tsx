import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen justify-center items-center font-geist-sans">
      hi and wait til this thing works (under construction fr fr)
    </main>
  );
}
